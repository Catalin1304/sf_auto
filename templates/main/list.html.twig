{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">

            <div class="col-xs-12">
{#                <div class="pull-right" style="margin-bottom: 10px; margin-top: 20px;">#}
{#                    <a href="{{ path('admin_genus_new') }}" class="btn btn-primary pull-right">Add <span class="fa fa-plus-circle"></span></a>#}
{#                </div>#}

                <h1>Masini</h1>

                <table class="table table-striped">
                    <tr>
                        <th style="padding-right: 20px;">Numar Inmatriculare</th>
                        <th style="padding-right: 20px;">Brand</th>
                        <th style="padding-right: 20px;">Proprietar</th>
                    </tr>
                    {% for vehicle in vehicles %}
                        <tr>
                            <td>{{ vehicle.plateNumber }}</td>
                            <td>{{ vehicle.brand.name }}</td>
                            <td>{{ vehicle.owner.name }}</td>
                        </tr>
                    {% endfor %}
                </table>
            </div>
        </div>
    </div>
{% endblock %}